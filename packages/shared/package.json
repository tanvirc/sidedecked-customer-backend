{
  "name": "@sidedecked/shared",
  "version": "1.0.0",
  "description": "Shared utilities for SideDecked Customer Backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "winston": "^3.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "typescript": "^5.2.2",
    "eslint": "^8.48.0"
  }
}