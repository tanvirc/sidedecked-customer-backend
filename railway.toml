[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run migration:deploy && node dist/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3