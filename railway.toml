[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && npm run migration:run && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3